# taskfile/flow | 811 func | 12 pipelines | 19 hub-types | 2026-03-07

PIPELINES[12] (IO:12):
  Src [IO]: list[str] → list[tuple[Path, int]]
              → run(task_names:list[str]) → bool        CC=2    pure ▶
              → _validate_pre_run(task_names:list[str]) → bool CC=6    IO
              → validate_before_run(config:TaskfileConfig, env_name:str | None, task_names:list[str] | None) → list[Issue] CC=2    mutation
              → check_placeholder_values(config:TaskfileConfig) → list[Issue] CC=4    mutation
              → _check_env_placeholders(env_name:str, env_obj, config:TaskfileConfig, taskfile_dir:Path) → list[Issue] CC=8    mutation
              → _load_env_file_vars(env_file_path:Path) → set[str] CC=6    IO
              → set(ctx, new_version, dry_run, force)   CC=8    IO
              → find_taskfile(start_dir:str | Path | None) → Path CC=6    pure
              → scan_nearby_taskfiles(start_dir:str | Path | None) → list[tuple[Path, int]] CC=3    mutation
              → _scan_subdirectories(start:Path, max_depth:int) → list[tuple[Path, int]] CC=8    mutation ■
              PURITY: 2/10 pure  BOTTLENECK: _check_env_placeholders(CC=8)

  Src [IO]: ctx → list[tuple[Path, int]]
              → api_openapi(ctx, output_path:str | None) CC=4    IO ▶
              → create_app(taskfile_path:str | None) → FastAPI CC=1    pure
              → _register_taskfile_routes(app:FastAPI) → None CC=1    IO
              → validate_taskfile(config:TaskfileConfig) → list[str] CC=2    mutation
              → _validate_circular_dependencies(config:TaskfileConfig) → list[str] CC=4    mutation
              → set(ctx, new_version, dry_run, force)   CC=8    IO
              → find_taskfile(start_dir:str | Path | None) → Path CC=6    pure
              → scan_nearby_taskfiles(start_dir:str | Path | None) → list[tuple[Path, int]] CC=3    mutation
              → _scan_subdirectories(start:Path, max_depth:int) → list[tuple[Path, int]] CC=8    mutation
              → _scan_dir_for_taskfiles(directory:Path, level:int) → list[tuple[Path, int]] CC=3    mutation ■
              PURITY: 2/10 pure  BOTTLENECK: set(CC=8)

  Src [IO]: ctx → str | None
              → deploy_cmd(ctx, compose_override)       CC=4    IO ▶
              → _execute_deploy_strategy(strategy:DeployStrategy) → None CC=5    IO
              → _deploy_quadlet(env, env_name, env_file_path, compose_path, var_overrides, dry_run) CC=1    pure
              → _quadlet_step1_generate(env, compose_path, env_file_path, var_overrides, dry_run) CC=2    IO
              → compose_to_quadlet(compose:ComposeFile, output_dir:str | Path, network_name:str, auto_update:bool, services_filter:list[str] | None, env:dict[str, str] | None, dns_fix:bool) → list[Path] CC=3    IO
              → _generate_container_files(outdir:Path, compose:ComposeFile, network_name:str, auto_update:bool, services_filter:list[str] | None, env:dict[str, str] | None) → tuple[list[Path], set[str]] CC=4    IO
              → generate_container_unit(service_name:str, service:ServiceConfig, network_name:str, auto_update:bool, env:dict[str, str] | None) → str CC=1    pure
              → _build_container_section(service_name:str, service:ServiceConfig, network_name:str, auto_update:bool, env:dict[str, str] | None) → list[str] CC=4    mutation
              → _build_container_podman_args(service:ServiceConfig, lines:list[str]) → None CC=4    mutation
              → _parse_cpus_limit(deploy:dict) → str | None CC=2    pure ■
              PURITY: 3/10 pure  BOTTLENECK: _execute_deploy_strategy(CC=5)

  Src [IO]: ctx → list[tuple[Path, int]]
              → e2e_cmd(ctx, check_only, url, port_web, port_landing) CC=9    IO ▶
              → _run_iac_checks(config, env) → list[E2EResult] CC=2    mutation
              → _check_taskfile_valid(config) → E2EResult CC=5    pure
              → validate_taskfile(config:TaskfileConfig) → list[str] CC=2    mutation
              → _validate_circular_dependencies(config:TaskfileConfig) → list[str] CC=4    mutation
              → set(ctx, new_version, dry_run, force)   CC=8    IO
              → find_taskfile(start_dir:str | Path | None) → Path CC=6    pure
              → scan_nearby_taskfiles(start_dir:str | Path | None) → list[tuple[Path, int]] CC=3    mutation
              → _scan_subdirectories(start:Path, max_depth:int) → list[tuple[Path, int]] CC=8    mutation
              → _scan_dir_for_taskfiles(directory:Path, level:int) → list[tuple[Path, int]] CC=3    mutation ■
              PURITY: 2/10 pure  BOTTLENECK: e2e_cmd(CC=9)

  Src [IO]: ? → list[tuple[Path, int]]
              → run_all_checks() → None                 CC=5    pure ▶
              → validate_taskfile_variables() → None    CC=2    pure
              → check_unresolved_variables(config:TaskfileConfig) → list[Issue] CC=3    mutation
              → _check_env_file_vars(env_file:str, required_vars:set[str]) → list[Issue] CC=9    IO
              → set(ctx, new_version, dry_run, force)   CC=8    IO
              → find_taskfile(start_dir:str | Path | None) → Path CC=6    pure
              → scan_nearby_taskfiles(start_dir:str | Path | None) → list[tuple[Path, int]] CC=3    mutation
              → _scan_subdirectories(start:Path, max_depth:int) → list[tuple[Path, int]] CC=8    mutation
              → _scan_dir_for_taskfiles(directory:Path, level:int) → list[tuple[Path, int]] CC=3    mutation ■
              PURITY: 3/9 pure  BOTTLENECK: _check_env_file_vars(CC=9)

  Src [IO]: TaskfileConfig → list[tuple[Path, int]]
              → check_dependent_files(config:TaskfileConfig) → list[Issue] CC=2    pure ▶
              → _check_circular_deps(config:TaskfileConfig) → list[Issue] CC=3    mutation
              → validate_taskfile(config:TaskfileConfig) → list[str] CC=2    mutation
              → _validate_circular_dependencies(config:TaskfileConfig) → list[str] CC=4    mutation
              → set(ctx, new_version, dry_run, force)   CC=8    IO
              → find_taskfile(start_dir:str | Path | None) → Path CC=6    pure
              → scan_nearby_taskfiles(start_dir:str | Path | None) → list[tuple[Path, int]] CC=3    mutation
              → _scan_subdirectories(start:Path, max_depth:int) → list[tuple[Path, int]] CC=8    mutation
              → _scan_dir_for_taskfiles(directory:Path, level:int) → list[tuple[Path, int]] CC=3    mutation ■
              PURITY: 2/9 pure  BOTTLENECK: set(CC=8)

  Src [IO]: ctx → list[tuple[Path, int]]
              → quadlet_generate(ctx, compose_path, env_file, output_dir, network, no_auto_update, services, dry_run) CC=13   IO ▶
              → compose_to_quadlet(compose:ComposeFile, output_dir:str | Path, network_name:str, auto_update:bool, services_filter:list[str] | None, env:dict[str, str] | None, dns_fix:bool) → list[Path] CC=3    IO
              → _generate_container_files(outdir:Path, compose:ComposeFile, network_name:str, auto_update:bool, services_filter:list[str] | None, env:dict[str, str] | None) → tuple[list[Path], set[str]] CC=4    IO
              → _collect_named_volumes(service:ServiceConfig) → set[str] CC=6    mutation
              → set(ctx, new_version, dry_run, force)   CC=8    IO
              → find_taskfile(start_dir:str | Path | None) → Path CC=6    pure
              → scan_nearby_taskfiles(start_dir:str | Path | None) → list[tuple[Path, int]] CC=3    mutation
              → _scan_subdirectories(start:Path, max_depth:int) → list[tuple[Path, int]] CC=8    mutation
              → _scan_dir_for_taskfiles(directory:Path, level:int) → list[tuple[Path, int]] CC=3    mutation ■
              PURITY: 1/9 pure  BOTTLENECK: quadlet_generate(CC=13)

  Src [IO]: HostContext → list[tuple[Path, int]]
              → check_all(ctx:HostContext, domains:list[str] | None, containers:list[str] | None) → list[Issue] CC=7    mutation ▶
              → check_ssh_connectivity() → None         CC=2    pure
              → _load_config()                          CC=3    mutation
              → load_taskfile(path:str | Path | None) → TaskfileConfig CC=5    IO
              → find_taskfile(start_dir:str | Path | None) → Path CC=6    pure
              → scan_nearby_taskfiles(start_dir:str | Path | None) → list[tuple[Path, int]] CC=3    mutation
              → _scan_subdirectories(start:Path, max_depth:int) → list[tuple[Path, int]] CC=8    mutation
              → _scan_dir_for_taskfiles(directory:Path, level:int) → list[tuple[Path, int]] CC=3    mutation ■
              PURITY: 2/8 pure  BOTTLENECK: _scan_subdirectories(CC=8)

  Src [IO]: ctx → list[tuple[Path, int]]
              → run(ctx, tasks, run_tags, explain, teach) CC=8    IO ▶
              → _run_tasks(opts:dict, task_list:list[str], tag_filter:list[str] | None) → bool CC=4    IO
              → _run_env_group(taskfile_path, env_group:str, task_names:list[str], platform_name:str | None, var_overrides:dict[str, str] | None, dry_run:bool, verbose:bool) → bool CC=6    IO
              → load_taskfile(path:str | Path | None) → TaskfileConfig CC=5    IO
              → find_taskfile(start_dir:str | Path | None) → Path CC=6    pure
              → scan_nearby_taskfiles(start_dir:str | Path | None) → list[tuple[Path, int]] CC=3    mutation
              → _scan_subdirectories(start:Path, max_depth:int) → list[tuple[Path, int]] CC=8    mutation
              → _scan_dir_for_taskfiles(directory:Path, level:int) → list[tuple[Path, int]] CC=3    mutation ■
              PURITY: 1/8 pure  BOTTLENECK: run(CC=8)

  Src [IO]: ctx → list[tuple[Path, int]]
              → validate(ctx, check_files, show_deps)   CC=14   IO ▶
              → validate_taskfile(config:TaskfileConfig) → list[str] CC=2    mutation
              → _validate_circular_dependencies(config:TaskfileConfig) → list[str] CC=4    mutation
              → set(ctx, new_version, dry_run, force)   CC=8    IO
              → find_taskfile(start_dir:str | Path | None) → Path CC=6    pure
              → scan_nearby_taskfiles(start_dir:str | Path | None) → list[tuple[Path, int]] CC=3    mutation
              → _scan_subdirectories(start:Path, max_depth:int) → list[tuple[Path, int]] CC=8    mutation
              → _scan_dir_for_taskfiles(directory:Path, level:int) → list[tuple[Path, int]] CC=3    mutation ■
              PURITY: 1/8 pure  BOTTLENECK: validate(CC=14)

  Src [IO]: fix → list[tuple[Path, int]]
              → doctor(fix, verbose, report, check_examples_flag, llm, category, teach, remote) CC=6    IO ▶
              → _doctor_run_checks(diagnostics:ProjectDiagnostics) → None CC=4    pure
              → _run_remote_diagnostics(diagnostics:ProjectDiagnostics) → None CC=4    IO
              → load_taskfile(path:str | Path | None) → TaskfileConfig CC=5    IO
              → find_taskfile(start_dir:str | Path | None) → Path CC=6    pure
              → scan_nearby_taskfiles(start_dir:str | Path | None) → list[tuple[Path, int]] CC=3    mutation
              → _scan_subdirectories(start:Path, max_depth:int) → list[tuple[Path, int]] CC=8    mutation
              → _scan_dir_for_taskfiles(directory:Path, level:int) → list[tuple[Path, int]] CC=3    mutation ■
              PURITY: 2/8 pure  BOTTLENECK: _scan_subdirectories(CC=8)

  Src [IO]: ctx → list[tuple[Path, int]]
              → graph(ctx, task_name, dot, output)      CC=7    IO ▶
              → print_task_tree(config:TaskfileConfig, root_task:str | None) → None CC=10   IO
              → detect_cycles(graph:dict[str, list[str]]) → list[str] | None CC=4    IO
              → set(ctx, new_version, dry_run, force)   CC=8    IO
              → find_taskfile(start_dir:str | Path | None) → Path CC=6    pure
              → scan_nearby_taskfiles(start_dir:str | Path | None) → list[tuple[Path, int]] CC=3    mutation
              → _scan_subdirectories(start:Path, max_depth:int) → list[tuple[Path, int]] CC=8    mutation
              → _scan_dir_for_taskfiles(directory:Path, level:int) → list[tuple[Path, int]] CC=3    mutation ■
              PURITY: 1/8 pure  BOTTLENECK: print_task_tree(CC=10)


TRANSFORMS (fan-out ≥10):
  _register_run_routes(app:FastAPI) → None                fan=23   !! side-effects
  prod(env_file)                                          fan=21   !! side-effects
  bump(ctx, part, dry_run, force)                         fan=20   !! side-effects
  _install_from_github(repo:str, version:str | None, save:bool) → Path fan=18   PIPELINE:RegistryClient.entry
  e2e_cmd(ctx, check_only, url, port_web, port_landing)   fan=18   fan=18
  run_all_checks() → None                                 fan=17   PIPELINE:ProjectDiagnostics.entry
  check_ufw_forward_policy() → list[Issue]                fan=16   fan=16
  set(ctx, new_version, dry_run, force)                   fan=16   fan=16
  auth_setup(registry)                                    fan=16   fan=16
  explain(ctx, task_name)                                 fan=16   fan=16
  quadlet_generate(ctx, compose_path, env_file, output_dir, network, no_auto_update, services, dry_run) fan=16   fan=16
  fleet_status_cmd(ctx, group)                            fan=16   fan=16
  fleet_repair_cmd(ctx, env_name, auto_fix)               fan=16   fan=16
  _register_metadata_routes(app:FastAPI) → None           fan=16   fan=16
  _scan_file_for_unresolved(filepath:Path, taskfile_dir:Path) → list[Issue] fan=15   fan=15

CONTRACTS:
  Pipeline: Src
    run(task_names:list[str]) → bool
      IN:  task_names:list[str]
      OUT: bool

    _validate_pre_run(task_names:list[str]) → bool
      IN:  task_names:list[str]
      OUT: bool
      SIDE-EFFECT: IO(print)
      INVARIANT: raises on invalid input

    validate_before_run(config:TaskfileConfig, env_name:str | None, task_names:list[str] | None) → list[Issue]
      IN:  config:TaskfileConfig, env_name:str | None, task_names:list[str] | None
      OUT: list[Issue]
      SIDE-EFFECT: pure
      INVARIANT: raises on invalid input

    check_placeholder_values(config:TaskfileConfig) → list[Issue]
      IN:  config:TaskfileConfig
      OUT: list[Issue]
      SIDE-EFFECT: pure
      INVARIANT: raises on invalid input

    _check_env_placeholders(env_name:str, env_obj, config:TaskfileConfig, taskfile_dir:Path) → list[Issue]
      IN:  env_name:str, env_obj, config:TaskfileConfig, taskfile_dir:Path
      OUT: list[Issue]
      SIDE-EFFECT: pure
      INVARIANT: raises on invalid input

    _load_env_file_vars(env_file_path:Path) → set[str]
      IN:  env_file_path:Path
      OUT: set[str]
      SIDE-EFFECT: IO(read_text)

    set(ctx, new_version, dry_run, force)
      IN:  ctx, new_version, dry_run, force
      OUT: None
      SIDE-EFFECT: IO(print, write_text, print)

    find_taskfile(start_dir:str | Path | None) → Path
      IN:  start_dir:str | Path | None
      OUT: Path

    scan_nearby_taskfiles(start_dir:str | Path | None) → list[tuple[Path, int]]
      IN:  start_dir:str | Path | None
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

    _scan_subdirectories(start:Path, max_depth:int) → list[tuple[Path, int]]
      IN:  start:Path, max_depth:int
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

  Pipeline: Src
    api_openapi(ctx, output_path:str | None)
      IN:  ctx, output_path:str | None
      OUT: None
      SIDE-EFFECT: IO(dumps, write_text, print)

    create_app(taskfile_path:str | None) → FastAPI
      IN:  taskfile_path:str | None
      OUT: FastAPI

    _register_taskfile_routes(app:FastAPI) → None
      IN:  app:FastAPI
      OUT: None
      SIDE-EFFECT: IO(loads, read_text)

    validate_taskfile(config:TaskfileConfig) → list[str]
      IN:  config:TaskfileConfig
      OUT: list[str]
      SIDE-EFFECT: pure
      INVARIANT: raises on invalid input

    _validate_circular_dependencies(config:TaskfileConfig) → list[str]
      IN:  config:TaskfileConfig
      OUT: list[str]
      SIDE-EFFECT: pure
      INVARIANT: raises on invalid input

    set(ctx, new_version, dry_run, force)
      IN:  ctx, new_version, dry_run, force
      OUT: None
      SIDE-EFFECT: IO(print, write_text, print)

    find_taskfile(start_dir:str | Path | None) → Path
      IN:  start_dir:str | Path | None
      OUT: Path

    scan_nearby_taskfiles(start_dir:str | Path | None) → list[tuple[Path, int]]
      IN:  start_dir:str | Path | None
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

    _scan_subdirectories(start:Path, max_depth:int) → list[tuple[Path, int]]
      IN:  start:Path, max_depth:int
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

    _scan_dir_for_taskfiles(directory:Path, level:int) → list[tuple[Path, int]]
      IN:  directory:Path, level:int
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

  Pipeline: Src
    deploy_cmd(ctx, compose_override)
      IN:  ctx, compose_override
      OUT: None
      SIDE-EFFECT: IO(print, print)

    _execute_deploy_strategy(strategy:DeployStrategy) → None
      IN:  strategy:DeployStrategy
      OUT: None
      SIDE-EFFECT: IO(print)

    _deploy_quadlet(env, env_name, env_file_path, compose_path, var_overrides, dry_run)
      IN:  env, env_name, env_file_path, compose_path, var_overrides, dry_run
      OUT: None

    _quadlet_step1_generate(env, compose_path, env_file_path, var_overrides, dry_run)
      IN:  env, compose_path, env_file_path, var_overrides, dry_run
      OUT: None
      SIDE-EFFECT: IO(print, print)

    compose_to_quadlet(compose:ComposeFile, output_dir:str | Path, network_name:str, auto_update:bool, services_filter:list[str] | None, env:dict[str, str] | None, dns_fix:bool) → list[Path]
      IN:  compose:ComposeFile, output_dir:str | Path, network_name:str, auto_update:bool, services_filter:list[str] | None, env:dict[str, str] | None, dns_fix:bool
      OUT: list[Path]
      SIDE-EFFECT: IO(mkdir)

    _generate_container_files(outdir:Path, compose:ComposeFile, network_name:str, auto_update:bool, services_filter:list[str] | None, env:dict[str, str] | None) → tuple[list[Path], set[str]]
      IN:  outdir:Path, compose:ComposeFile, network_name:str, auto_update:bool, services_filter:list[str] | None, env:dict[str, str] | None
      OUT: tuple[list[Path], set[str]]
      SIDE-EFFECT: IO(write_text, print)

    generate_container_unit(service_name:str, service:ServiceConfig, network_name:str, auto_update:bool, env:dict[str, str] | None) → str
      IN:  service_name:str, service:ServiceConfig, network_name:str, auto_update:bool, env:dict[str, str] | None
      OUT: str

    _build_container_section(service_name:str, service:ServiceConfig, network_name:str, auto_update:bool, env:dict[str, str] | None) → list[str]
      IN:  service_name:str, service:ServiceConfig, network_name:str, auto_update:bool, env:dict[str, str] | None
      OUT: list[str]
      SIDE-EFFECT: pure

    _build_container_podman_args(service:ServiceConfig, lines:list[str]) → None
      IN:  service:ServiceConfig, lines:list[str]
      OUT: None
      SIDE-EFFECT: pure

    _parse_cpus_limit(deploy:dict) → str | None
      IN:  deploy:dict
      OUT: str | None

  Pipeline: Src
    e2e_cmd(ctx, check_only, url, port_web, port_landing)
      IN:  ctx, check_only, url, port_web, port_landing
      OUT: None
      SIDE-EFFECT: IO(print, print, print)

    _run_iac_checks(config, env) → list[E2EResult]
      IN:  config, env
      OUT: list[E2EResult]
      SIDE-EFFECT: pure
      INVARIANT: raises on invalid input

    _check_taskfile_valid(config) → E2EResult
      IN:  config
      OUT: E2EResult
      INVARIANT: raises on invalid input

    validate_taskfile(config:TaskfileConfig) → list[str]
      IN:  config:TaskfileConfig
      OUT: list[str]
      SIDE-EFFECT: pure
      INVARIANT: raises on invalid input

    _validate_circular_dependencies(config:TaskfileConfig) → list[str]
      IN:  config:TaskfileConfig
      OUT: list[str]
      SIDE-EFFECT: pure
      INVARIANT: raises on invalid input

    set(ctx, new_version, dry_run, force)
      IN:  ctx, new_version, dry_run, force
      OUT: None
      SIDE-EFFECT: IO(print, write_text, print)

    find_taskfile(start_dir:str | Path | None) → Path
      IN:  start_dir:str | Path | None
      OUT: Path

    scan_nearby_taskfiles(start_dir:str | Path | None) → list[tuple[Path, int]]
      IN:  start_dir:str | Path | None
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

    _scan_subdirectories(start:Path, max_depth:int) → list[tuple[Path, int]]
      IN:  start:Path, max_depth:int
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

    _scan_dir_for_taskfiles(directory:Path, level:int) → list[tuple[Path, int]]
      IN:  directory:Path, level:int
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

  Pipeline: Src
    run_all_checks() → None
      IN:  ()
      OUT: None
      INVARIANT: raises on invalid input

    validate_taskfile_variables() → None
      IN:  ()
      OUT: None
      INVARIANT: raises on invalid input

    check_unresolved_variables(config:TaskfileConfig) → list[Issue]
      IN:  config:TaskfileConfig
      OUT: list[Issue]
      SIDE-EFFECT: pure
      INVARIANT: all entities exist in analysis

    _check_env_file_vars(env_file:str, required_vars:set[str]) → list[Issue]
      IN:  env_file:str, required_vars:set[str]
      OUT: list[Issue]
      SIDE-EFFECT: IO(read_text)
      INVARIANT: raises on invalid input

    set(ctx, new_version, dry_run, force)
      IN:  ctx, new_version, dry_run, force
      OUT: None
      SIDE-EFFECT: IO(print, write_text, print)

    find_taskfile(start_dir:str | Path | None) → Path
      IN:  start_dir:str | Path | None
      OUT: Path

    scan_nearby_taskfiles(start_dir:str | Path | None) → list[tuple[Path, int]]
      IN:  start_dir:str | Path | None
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

    _scan_subdirectories(start:Path, max_depth:int) → list[tuple[Path, int]]
      IN:  start:Path, max_depth:int
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

    _scan_dir_for_taskfiles(directory:Path, level:int) → list[tuple[Path, int]]
      IN:  directory:Path, level:int
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

  Pipeline: Src
    check_dependent_files(config:TaskfileConfig) → list[Issue]
      IN:  config:TaskfileConfig
      OUT: list[Issue]
      INVARIANT: raises on invalid input

    _check_circular_deps(config:TaskfileConfig) → list[Issue]
      IN:  config:TaskfileConfig
      OUT: list[Issue]
      SIDE-EFFECT: pure
      INVARIANT: raises on invalid input

    validate_taskfile(config:TaskfileConfig) → list[str]
      IN:  config:TaskfileConfig
      OUT: list[str]
      SIDE-EFFECT: pure
      INVARIANT: raises on invalid input

    _validate_circular_dependencies(config:TaskfileConfig) → list[str]
      IN:  config:TaskfileConfig
      OUT: list[str]
      SIDE-EFFECT: pure
      INVARIANT: raises on invalid input

    set(ctx, new_version, dry_run, force)
      IN:  ctx, new_version, dry_run, force
      OUT: None
      SIDE-EFFECT: IO(print, write_text, print)

    find_taskfile(start_dir:str | Path | None) → Path
      IN:  start_dir:str | Path | None
      OUT: Path

    scan_nearby_taskfiles(start_dir:str | Path | None) → list[tuple[Path, int]]
      IN:  start_dir:str | Path | None
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

    _scan_subdirectories(start:Path, max_depth:int) → list[tuple[Path, int]]
      IN:  start:Path, max_depth:int
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

    _scan_dir_for_taskfiles(directory:Path, level:int) → list[tuple[Path, int]]
      IN:  directory:Path, level:int
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

  Pipeline: Src
    quadlet_generate(ctx, compose_path, env_file, output_dir, network, no_auto_update, services, dry_run)
      IN:  ctx, compose_path, env_file, output_dir, network, no_auto_update, services, dry_run
      OUT: None
      SIDE-EFFECT: IO(print, print, print)

    compose_to_quadlet(compose:ComposeFile, output_dir:str | Path, network_name:str, auto_update:bool, services_filter:list[str] | None, env:dict[str, str] | None, dns_fix:bool) → list[Path]
      IN:  compose:ComposeFile, output_dir:str | Path, network_name:str, auto_update:bool, services_filter:list[str] | None, env:dict[str, str] | None, dns_fix:bool
      OUT: list[Path]
      SIDE-EFFECT: IO(mkdir)

    _generate_container_files(outdir:Path, compose:ComposeFile, network_name:str, auto_update:bool, services_filter:list[str] | None, env:dict[str, str] | None) → tuple[list[Path], set[str]]
      IN:  outdir:Path, compose:ComposeFile, network_name:str, auto_update:bool, services_filter:list[str] | None, env:dict[str, str] | None
      OUT: tuple[list[Path], set[str]]
      SIDE-EFFECT: IO(write_text, print)

    _collect_named_volumes(service:ServiceConfig) → set[str]
      IN:  service:ServiceConfig
      OUT: set[str]
      SIDE-EFFECT: pure

    set(ctx, new_version, dry_run, force)
      IN:  ctx, new_version, dry_run, force
      OUT: None
      SIDE-EFFECT: IO(print, write_text, print)

    find_taskfile(start_dir:str | Path | None) → Path
      IN:  start_dir:str | Path | None
      OUT: Path

    scan_nearby_taskfiles(start_dir:str | Path | None) → list[tuple[Path, int]]
      IN:  start_dir:str | Path | None
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

    _scan_subdirectories(start:Path, max_depth:int) → list[tuple[Path, int]]
      IN:  start:Path, max_depth:int
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

    _scan_dir_for_taskfiles(directory:Path, level:int) → list[tuple[Path, int]]
      IN:  directory:Path, level:int
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

  Pipeline: Src
    check_all(ctx:HostContext, domains:list[str] | None, containers:list[str] | None) → list[Issue]
      IN:  ctx:HostContext, domains:list[str] | None, containers:list[str] | None
      OUT: list[Issue]
      SIDE-EFFECT: pure
      INVARIANT: raises on invalid input

    check_ssh_connectivity() → None
      IN:  ()
      OUT: None
      INVARIANT: raises on invalid input

    _load_config()
      IN:  ()
      OUT: None
      SIDE-EFFECT: mutates self._config, self._config

    load_taskfile(path:str | Path | None) → TaskfileConfig
      IN:  path:str | Path | None
      OUT: TaskfileConfig
      SIDE-EFFECT: IO(open)

    find_taskfile(start_dir:str | Path | None) → Path
      IN:  start_dir:str | Path | None
      OUT: Path

    scan_nearby_taskfiles(start_dir:str | Path | None) → list[tuple[Path, int]]
      IN:  start_dir:str | Path | None
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

    _scan_subdirectories(start:Path, max_depth:int) → list[tuple[Path, int]]
      IN:  start:Path, max_depth:int
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

    _scan_dir_for_taskfiles(directory:Path, level:int) → list[tuple[Path, int]]
      IN:  directory:Path, level:int
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

  Pipeline: Src
    run(ctx, tasks, run_tags, explain, teach)
      IN:  ctx, tasks, run_tags, explain, teach
      OUT: None
      SIDE-EFFECT: IO(print)

    _run_tasks(opts:dict, task_list:list[str], tag_filter:list[str] | None) → bool
      IN:  opts:dict, task_list:list[str], tag_filter:list[str] | None
      OUT: bool
      SIDE-EFFECT: IO(print)

    _run_env_group(taskfile_path, env_group:str, task_names:list[str], platform_name:str | None, var_overrides:dict[str, str] | None, dry_run:bool, verbose:bool) → bool
      IN:  taskfile_path, env_group:str, task_names:list[str], platform_name:str | None, var_overrides:dict[str, str] | None, dry_run:bool, verbose:bool
      OUT: bool
      SIDE-EFFECT: IO(print, print, print)

    load_taskfile(path:str | Path | None) → TaskfileConfig
      IN:  path:str | Path | None
      OUT: TaskfileConfig
      SIDE-EFFECT: IO(open)

    find_taskfile(start_dir:str | Path | None) → Path
      IN:  start_dir:str | Path | None
      OUT: Path

    scan_nearby_taskfiles(start_dir:str | Path | None) → list[tuple[Path, int]]
      IN:  start_dir:str | Path | None
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

    _scan_subdirectories(start:Path, max_depth:int) → list[tuple[Path, int]]
      IN:  start:Path, max_depth:int
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

    _scan_dir_for_taskfiles(directory:Path, level:int) → list[tuple[Path, int]]
      IN:  directory:Path, level:int
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

  Pipeline: Src
    validate(ctx, check_files, show_deps)
      IN:  ctx, check_files, show_deps
      OUT: None
      SIDE-EFFECT: IO(print, print, print)
      INVARIANT: raises on invalid input

    validate_taskfile(config:TaskfileConfig) → list[str]
      IN:  config:TaskfileConfig
      OUT: list[str]
      SIDE-EFFECT: pure
      INVARIANT: raises on invalid input

    _validate_circular_dependencies(config:TaskfileConfig) → list[str]
      IN:  config:TaskfileConfig
      OUT: list[str]
      SIDE-EFFECT: pure
      INVARIANT: raises on invalid input

    set(ctx, new_version, dry_run, force)
      IN:  ctx, new_version, dry_run, force
      OUT: None
      SIDE-EFFECT: IO(print, write_text, print)

    find_taskfile(start_dir:str | Path | None) → Path
      IN:  start_dir:str | Path | None
      OUT: Path

    scan_nearby_taskfiles(start_dir:str | Path | None) → list[tuple[Path, int]]
      IN:  start_dir:str | Path | None
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

    _scan_subdirectories(start:Path, max_depth:int) → list[tuple[Path, int]]
      IN:  start:Path, max_depth:int
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

    _scan_dir_for_taskfiles(directory:Path, level:int) → list[tuple[Path, int]]
      IN:  directory:Path, level:int
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

  Pipeline: Src
    doctor(fix, verbose, report, check_examples_flag, llm, category, teach, remote)
      IN:  fix, verbose, report, check_examples_flag, llm, category, teach, remote
      OUT: None
      SIDE-EFFECT: IO(print)

    _doctor_run_checks(diagnostics:ProjectDiagnostics) → None
      IN:  diagnostics:ProjectDiagnostics
      OUT: None
      INVARIANT: raises on invalid input

    _run_remote_diagnostics(diagnostics:ProjectDiagnostics) → None
      IN:  diagnostics:ProjectDiagnostics
      OUT: None
      SIDE-EFFECT: IO(print, print)

    load_taskfile(path:str | Path | None) → TaskfileConfig
      IN:  path:str | Path | None
      OUT: TaskfileConfig
      SIDE-EFFECT: IO(open)

    find_taskfile(start_dir:str | Path | None) → Path
      IN:  start_dir:str | Path | None
      OUT: Path

    scan_nearby_taskfiles(start_dir:str | Path | None) → list[tuple[Path, int]]
      IN:  start_dir:str | Path | None
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

    _scan_subdirectories(start:Path, max_depth:int) → list[tuple[Path, int]]
      IN:  start:Path, max_depth:int
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

    _scan_dir_for_taskfiles(directory:Path, level:int) → list[tuple[Path, int]]
      IN:  directory:Path, level:int
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

  Pipeline: Src
    graph(ctx, task_name, dot, output)
      IN:  ctx, task_name, dot, output
      OUT: None
      SIDE-EFFECT: IO(print, print, print)

    print_task_tree(config:TaskfileConfig, root_task:str | None) → None
      IN:  config:TaskfileConfig, root_task:str | None
      OUT: None
      SIDE-EFFECT: IO(print, print, print)

    detect_cycles(graph:dict[str, list[str]]) → list[str] | None
      IN:  graph:dict[str, list[str]]
      OUT: list[str] | None
      SIDE-EFFECT: IO(remove)

    set(ctx, new_version, dry_run, force)
      IN:  ctx, new_version, dry_run, force
      OUT: None
      SIDE-EFFECT: IO(print, write_text, print)

    find_taskfile(start_dir:str | Path | None) → Path
      IN:  start_dir:str | Path | None
      OUT: Path

    scan_nearby_taskfiles(start_dir:str | Path | None) → list[tuple[Path, int]]
      IN:  start_dir:str | Path | None
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

    _scan_subdirectories(start:Path, max_depth:int) → list[tuple[Path, int]]
      IN:  start:Path, max_depth:int
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure

    _scan_dir_for_taskfiles(directory:Path, level:int) → list[tuple[Path, int]]
      IN:  directory:Path, level:int
      OUT: list[tuple[Path, int]]
      SIDE-EFFECT: pure


DATA_TYPES (by cross-function usage) [744 annotated, 0 inferred / 811 functions]:
  str                  consumed:449 produced:85   !! HUB-TYPE → split interface
  bool                 consumed:87  produced:98   !! HUB-TYPE → split interface
  int                  consumed:87  produced:30   !! HUB-TYPE → split interface
  dict                 consumed:65  produced:31   !! HUB-TYPE → split interface
  list[str]            consumed:43  produced:41   !! HUB-TYPE → split interface
  str | None           consumed:59  produced:20   !! HUB-TYPE → split interface
  TaskfileConfig       consumed:50  produced:3    !! HUB-TYPE → split interface
  Path                 consumed:41  produced:10   !! HUB-TYPE → split interface
  list[Issue]          consumed:10  produced:37   !! HUB-TYPE → split interface
  Task                 consumed:42  produced:0    !! HUB-TYPE → split interface
  dict[str, str]       consumed:18  produced:15   !! HUB-TYPE → split interface
  list[str] | None     consumed:22  produced:1    !! HUB-TYPE → split interface
  Issue                consumed:11  produced:5    !! HUB-TYPE → split interface
  str | Path           consumed:14  produced:0    !! HUB-TYPE → split interface
  dict[str, str] | None consumed:13  produced:0    !! HUB-TYPE → split interface
  str | Path | None    consumed:13  produced:0    !! HUB-TYPE → split interface
  FleetConfig          consumed:11  produced:2    !! HUB-TYPE → split interface
  float                consumed:12  produced:0    !! HUB-TYPE → split interface
  E2EResult            consumed:0   produced:11   output-heavy
  ServiceConfig        consumed:11  produced:0    !! HUB-TYPE → split interface

  HUB TYPES (consumed ≥10):
    str → 449 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)
    bool → 87 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)
    int → 87 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)
    dict → 65 consumers → split into:
      - replace with typed alternatives (dataclass/TypedDict)
    list[str] → 43 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)
    str | None → 59 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)
    TaskfileConfig → 50 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)
    Path → 41 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)
    list[Issue] → 10 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)
    Task → 42 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)
    dict[str, str] → 18 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)
    list[str] | None → 22 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)
    Issue → 11 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)
    str | Path → 14 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)
    dict[str, str] | None → 13 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)
    str | Path | None → 13 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)
    FleetConfig → 11 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)
    float → 12 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)
    ServiceConfig → 11 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)

SIDE_EFFECTS:
  IO:        VPSProvisioner.provision, VPSProvisioner._system_update, VPSProvisioner._install_podman, VPSProvisioner._setup_firewall, VPSProvisioner._create_deploy_user, VPSProvisioner._setup_traefik, VPSProvisioner._setup_tls, provision_vps, generate_container_resolv_conf, GitHubActionsConverter.export_workflow
  Cache:     TaskCache.__init__, TaskCache.save, TaskCache.clear, TaskCache.get_stats
  Mutation:  check_all, VPSProvisioner.__init__, check_host_dns, check_container_dns, check_systemd_resolved, expand_deploy_recipe, _deploy_task, _rollback_tasks, _post_deploy_health_cmds, _quadlet_deploy
  Pure:      VPSProvisioner._ssh, VPSProvisioner._check_command, VPSProvisioner.is_provisioned, fix_resolv_conf, fix_disable_systemd_resolved, _build_tasks, _push_tasks, _validate_task, _health_tasks, _graceful_restart_cmds

  PIPELINE PURITY:
    Src             ░░░░ 20% pure
    Src             ░░░░ 20% pure
    Src             █░░░ 30% pure
    Src             ░░░░ 20% pure
    Src             █░░░ 33% pure
    Src             ░░░░ 22% pure
    Src             ░░░░ 11% pure
    Src             █░░░ 25% pure
    Src             ░░░░ 12% pure
    Src             ░░░░ 12% pure
    Src             █░░░ 25% pure
    Src             ░░░░ 12% pure
