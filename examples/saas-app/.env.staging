# ─── saas-app — STAGING ─────────────────────────────
# SSH/runtime/env_file are auto-inferred by smart defaults
# (ssh_host → podman, quadlet, ~/.ssh/id_ed25519, .env.staging)
DOMAIN=staging.example.com
APP_NAME=saas-app
VERSION=0.1.0
PORT_WEB=8000
PORT_API=8080
DATABASE_URL=postgresql://user:pass@staging-db/db
REGISTRY=ghcr.io/myorg
TAG=latest
