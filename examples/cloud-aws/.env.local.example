# cloud-aws — Local template
# Copy to .env.local and adjust values
# No 'local' env in Taskfile — use 'dev' for local AWS development
APP_NAME=cloud-app
VERSION=0.1.0
DOMAIN=localhost
AWS_REGION=eu-west-1
AWS_PROFILE=dev
TAG=dev
