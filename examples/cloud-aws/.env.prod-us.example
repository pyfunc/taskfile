# cloud-aws — Production US template
# Copy to .env.prod-us and fill in secrets
# Smart defaults: env_file → .env.prod-us (auto-inferred)
APP_NAME=cloud-app
VERSION=0.1.0
DOMAIN=us.example.com
AWS_REGION=us-east-1
AWS_PROFILE=prod-us
ECS_DESIRED_COUNT=4
LAMBDA_MEMORY=1024
TAG=latest
