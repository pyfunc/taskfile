# cloud-aws — Dev template
# Copy to .env.dev and adjust values
# Smart defaults: env_file → .env.dev (auto-inferred)
APP_NAME=cloud-app
VERSION=0.1.0
DOMAIN=dev.example.com
AWS_REGION=eu-west-1
AWS_PROFILE=dev
ECS_DESIRED_COUNT=1
LAMBDA_MEMORY=256
TAG=dev
