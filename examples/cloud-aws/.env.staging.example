# cloud-aws — Staging template
# Copy to .env.staging and adjust values
# Smart defaults: env_file → .env.staging (auto-inferred)
APP_NAME=cloud-app
VERSION=0.1.0
DOMAIN=staging.example.com
AWS_REGION=eu-west-1
AWS_PROFILE=staging
ECS_DESIRED_COUNT=2
LAMBDA_MEMORY=512
TAG=latest
