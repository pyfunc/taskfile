# ─── cloud-aws — LOCAL (dev alias) ──────────────────
# No 'local' env in Taskfile — use 'dev' for local AWS development
# This file kept for docker compose local testing
APP_NAME=cloud-app
VERSION=0.1.0
DOMAIN=localhost
AWS_REGION=eu-west-1
AWS_PROFILE=dev
TAG=dev
