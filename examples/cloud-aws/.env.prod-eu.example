# cloud-aws — Production EU template
# Copy to .env.prod-eu and fill in secrets
# Smart defaults: env_file → .env.prod-eu (auto-inferred)
APP_NAME=cloud-app
VERSION=0.1.0
DOMAIN=eu.example.com
AWS_REGION=eu-west-1
AWS_PROFILE=prod
ECS_DESIRED_COUNT=4
LAMBDA_MEMORY=1024
TAG=latest
