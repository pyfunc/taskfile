# monorepo-microservices — Production template
# Copy to .env.prod and fill in secrets
# Smart defaults: ssh_host → podman/quadlet, env_file → .env.prod
APP_NAME=platform
VERSION=0.1.0
DOMAIN=example.com
REGISTRY=ghcr.io/myorg
TAG=latest
