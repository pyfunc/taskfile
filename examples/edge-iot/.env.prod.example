# edge-iot — Production gateway template
# Copy to .env.prod and fill in secrets
# hosts: auto-inferred: podman/quadlet, env_file per gateway type
APP_NAME=iot-gateway
VERSION=0.1.0
MQTT_BROKER=mqtt.example.com
MQTT_PORT=1883
LOG_LEVEL=warn
FIRMWARE_CHANNEL=stable
