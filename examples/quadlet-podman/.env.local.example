# quadlet-podman — Local development template
# Copy to .env.local and adjust values
# Smart defaults: no ssh_host → docker/compose, env_file → .env.local
APP_NAME=webapp
VERSION=0.1.0
PORT_WEB=8000
DOMAIN=localhost
REGISTRY=ghcr.io/myorg
TAG=dev
