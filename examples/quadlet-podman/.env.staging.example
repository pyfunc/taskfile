# quadlet-podman — Staging template
# Copy to .env.staging and adjust values
# Smart defaults: ssh_host → podman/quadlet, env_file → .env.staging
APP_NAME=webapp
VERSION=0.1.0
DOMAIN=staging.example.com
REGISTRY=ghcr.io/myorg
TAG=latest
