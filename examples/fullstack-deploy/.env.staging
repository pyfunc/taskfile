# ─── fullstack-deploy — STAGING ─────────────────────
# Smart defaults: ssh_host → docker/compose (overridden), env_file → .env.staging
APP_NAME=fullstack-app
VERSION=0.1.0
DOMAIN=staging.example.com
API_URL=https://api.staging.example.com
REGISTRY=ghcr.io/myorg
TAG=latest
