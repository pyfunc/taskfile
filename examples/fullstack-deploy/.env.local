# ─── fullstack-deploy — LOCAL DEVELOPMENT ───────────
# Smart defaults: no ssh_host → docker/compose, env_file → .env.local
APP_NAME=fullstack-app
VERSION=0.1.0
PORT_WEB=8000
DOMAIN=localhost
API_URL=http://localhost:8080
REGISTRY=ghcr.io/myorg
TAG=dev
