# Auto-generated from Taskfile.yml — do not edit manually
# Regenerate with: taskfile ci generate

stages:
- test
- build
- generate
- deploy
default:
  image: python:3.12-slim
test:
  stage: test
  script:
  - pip install taskfile
  - taskfile --env local run build --var TAG=$CI_COMMIT_SHORT_SHA
  only: &id001
  - main
build:
  stage: build
  script:
  - taskfile --env local run push --var TAG=$CI_COMMIT_SHORT_SHA
  image: docker:latest
  services:
  - docker:dind
  variables:
    DOCKER_TLS_CERTDIR: /certs
  before_script:
  - apk add --no-cache python3 py3-pip
  - pip install taskfile
  only: *id001
generate:
  stage: generate
  script:
  - pip install taskfile
  - taskfile --env local run generate --var TAG=$CI_COMMIT_SHORT_SHA
  only: *id001
  artifacts:
    paths:
    - deploy/quadlet/
deploy:
  stage: deploy
  script:
  - pip install taskfile
  - taskfile --env prod run deploy-quick --var TAG=$CI_COMMIT_SHORT_SHA
  when: manual
  before_script:
  - mkdir -p ~/.ssh
  - echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_ed25519
  - chmod 600 ~/.ssh/id_ed25519
